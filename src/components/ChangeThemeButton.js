import React from "react";

const ChangeThemeButton = () => {
    return (
        <div>
            <button className="btn toggle-btn">Change Theme</button>
        </div>
    );
};

export default ChangeThemeButton;